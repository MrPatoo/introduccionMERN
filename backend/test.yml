config:
  target: "http://localhost:4000"
  phases: 
  - duration: 60
    arrivalRate: 600
scenarios:
  - flow:
    - get: 
        url: "/api/products"
    - post:
        url: "/api/login"
        json:
          email: "ejemplo@example.com"
          password: "12345678"